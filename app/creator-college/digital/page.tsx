import "../creator-college.css";
import CreatorCollegeClient from "../CreatorCollegeClient";

export const metadata = {
  title: "Digital Product Builder | Creator College",
  description: "Turn an idea into a finished guide, checklist, workbook, planner or resource kit.",
};

export default function DigitalProductBuilderPage() {
  return <CreatorCollegeClient />;
}
